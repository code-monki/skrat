# CMake / out-of-tree build trees
/build/
/out/
/cmake-build-*/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps/

# In-tree CMake configure leftovers (if someone configures in repo root)
CMakeOutput.log
CMakeError.log

# Local build and tooling logs (repo root and typical names)
/*.log
build.log

# macOS iconutil scratch dir (skrat.icns is committed; iconset is not)
packaging/macos/skrat.iconset/

# Qt / IDE
*.user
CMakeLists.txt.user
*.autosave
*.qmlc
*.jsc
moc_*.cpp
ui_*.h
qrc_*.cpp
*~

# Editors
.idea/
.vs/
*.swp
*.swo
*.swn

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux / desktop environments
.directory

# Local Makefile overrides (paths to Qt / CMake on this machine)
/config.local.mk

# Homebrew Bundle — lockfile is machine-specific unless your team commits it on purpose
/Brewfile.lock.json

# Chocolatey — local `choco pack` output at repo root
/*.nupkg

# Node / npm (optional scripts or tooling alongside Qt/C++)
node_modules/
.npm/
*.tgz
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
